import { AddBackgroundDirective } from './add-background.directive';

describe('AddBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
